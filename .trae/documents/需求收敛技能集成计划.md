# 需求收敛技能集成计划

## 📋 背景
用户已将 `requirement-convergence` 技能移动到 `ai-contest-web/server/services/requirement-convergence` 目录下。本计划用于完成后续的 API 路由创建、主路由更新和服务集成。

## 🎯 目标
将需求收敛技能完整集成到 `ai-contest-web` 项目中，使前端可以通过 API 调用技能的各项功能。

---

## 📝 实施步骤

### Step 1: 创建 API 路由文件
**文件**: `server/routes/requirement.js`

**内容**:
- 创建 Express 路由
- 实现需求分析接口 `/api/requirement/analyze`
- 实现需求验证接口 `/api/requirement/validate`
- 实现需求图谱接口 `/api/requirement/graph`
- 实现智能推荐接口 `/api/requirement/recommend`
- 错误处理和日志记录

**依赖**: 无

---

### Step 2: 更新主路由配置
**文件**: `server/routes/index.js`

**操作**:
1. 导入 `requirement` 路由
2. 注册路由到 Express 应用
3. 确保路由前缀为 `/api/requirement`

**依赖**: Step 1 完成

---

### Step 3: 更新 skillService.js
**文件**: `server/services/skillService.js`

**操作**:
1. 导入需求收敛技能的 TypeScript 模块（需要 ts-node 或编译后的 JS）
2. 在 SkillService 类中添加需求收敛相关方法
3. 实现技能调用逻辑

**关键技术点**:
- TypeScript 模块在 Node.js 中的加载方式
- 可能需要配置 tsconfig.json
- 或者预编译 TypeScript 为 JavaScript

**依赖**: 无

---

### Step 4: 创建 TypeScript 配置
**文件**: `server/services/requirement-convergence/tsconfig.json`

**内容**:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**依赖**: 无

---

### Step 5: 创建统一导出入口
**文件**: `server/services/requirement-convergence/src/index.ts`

**内容**:
- 统一导出所有核心模块
- 提供简化的 API 接口
- 处理模块间的依赖关系

**依赖**: 无

---

### Step 6: 安装必要依赖
**操作**:
```bash
cd ai-contest-web
npm install typescript ts-node @types/node @types/express --save-dev
```

**依赖**: 无

---

### Step 7: 更新 package.json 脚本
**文件**: `package.json`

**操作**:
- 添加 TypeScript 编译脚本
- 添加技能构建脚本

**依赖**: Step 4 完成

---

### Step 8: 测试验证
**操作**:
1. 启动服务器
2. 测试 API 接口
3. 验证前端调用
4. 检查日志和错误

**测试用例**:
- POST `/api/requirement/analyze` - 需求分析
- POST `/api/requirement/validate` - 需求验证
- GET `/api/requirement/health` - 健康检查

**依赖**: Step 1-7 完成

---

## 📁 最终目录结构

```
ai-contest-web/
├── server/
│   ├── services/
│   │   ├── skillService.js
│   │   └── requirement-convergence/
│   │       ├── src/                      # 核心源代码
│   │       ├── docs/                     # 文档
│   │       ├── SKILL.md                  # 技能定义
│   │       ├── tsconfig.json             # TypeScript 配置
│   │       └── index.ts                  # 统一导出入口
│   │
│   ├── routes/
│   │   ├── index.js
│   │   └── requirement.js                # ✨ 新增
│   └── ...
│
├── src/
│   └── views/
│       └── requirement/                  # 前端界面
│
└── package.json
```

---

## ⚠️ 注意事项

1. **TypeScript 编译**: 确保 ts-node 正确配置，或预编译为 JavaScript
2. **模块路径**: 所有导入路径使用相对路径
3. **错误处理**: API 接口需要完善的错误处理和日志
4. **性能优化**: 考虑缓存机制，避免重复计算
5. **类型安全**: 保持 TypeScript 类型定义完整

---

## 🧪 验收标准

- [ ] API 路由文件创建完成
- [ ] 主路由配置更新完成
- [ ] skillService.js 集成完成
- [ ] TypeScript 配置正确
- [ ] 统一导出入口创建
- [ ] 依赖安装完成
- [ ] API 接口测试通过
- [ ] 前端可以正常调用

---

*创建时间：2026-03-17*
